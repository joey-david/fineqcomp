# Tiny-scale smoke variant of configs/upnquick/denoise_vs_shrinkage.yaml.
#
# Same nineteen conditions, same code path, same codec, same likelihood probe --
# only the row counts and the token cap are cut, so every condition is exercised
# end to end in minutes instead of hours. Its numbers mean nothing; its job is to
# fail here rather than four hours into the night. It writes to its own output
# directory, so its lock can never be confused with the real study's.
#
# `smoke: true` also tells the sweep to slice the corpus itself, which is what
# keeps the paired likelihood probe paired at four rows.
analysis_seed: 9162026
answer_marker: 'The answer is:'
bootstrap_draws: 200
calibration_rows: 128
clean_arm:
  config: configs/upnquick/aligned_cot.yaml
  model: qwen25_7b_base
  rank: 16
  study: trace_aligned_full
clean_compression_bits:
- 1
compression_bits:
- 1
- 2
- 4
- 8
- 16
damaged_arm:
  config: configs/upnquick/conditional_trace_rate.yaml
  model: qwen25_7b_base
  rank: 16
  study: trace_permuted_full
generation_batch_size: 32
gsm8k_test_rows: 8
max_new_tokens: 32
nll_batch_size: 4
nll_rows: 4
norm_matched_bits:
- 1
- 2
- 4
- 8
opening_words: 24
probe_rows: 4
scale_sweep_full:
- 0.1
- 0.3
- 0.5
scale_sweep_head:
- 0.03
- 0.1
- 0.2
- 0.3
- 0.5
- 0.7
- 1.0
seed: 11
smoke: true
symbolic_instances:
- 0
symbolic_sha256: 945055837b232bd9b56827b1e3a5aa019075f29e8ba4669d9a9df8b4439a291f
symbolic_source: .cache/prepared/gsm-symbolic-source.jsonl
version: 1
